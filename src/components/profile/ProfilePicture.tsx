import React from "react";
import ProfileImg from "../../img/profile.png";

const ProfilePicture = () => {
  return <img className="rounded-[10px]" src={ProfileImg} />;
};

export default ProfilePicture;
